Here is the prompt you can use with Gemini, ChatGPT, or any other LLM to process images or raw text into the exact JSON format your website requires.

Prompt for Math Problem Extraction & Formatting

Instructions:
You are a mathematical data extraction assistant. Your task is to analyze the provided input (which may be an image of a math problem, a photo of handwritten notes, or raw text) and convert it into a specific bilingual JSON structure.

Input:
[INSERT IMAGE OR TEXT HERE]

Requirements:

Extract the math problem accurately.

Solve the problem step-by-step.

Translate the problem, category, and solution steps into both English (_en) and Arabic (_ar).

Format the output as a JSON object matching the schema below exactly.

JSON Schema:

code
JSON
download
content_copy
expand_less
{
  "questions": [
    {
      "id": "UNIQUE_ID_TIMESTAMP",
      "category_en": "Algebra | Geometry | Calculus | Statistics | etc.",
      "category_ar": "الجبر | هندسة | تفاضل | إحصاء | إلخ",
      "difficulty": "Easy | Medium | Hard",
      "question_en": "The math problem in English",
      "question_ar": "المسألة الرياضية باللغة العربية",
      "answer": "Final numeric or symbolic answer only",
      "steps_en": [
        "Step 1 explanation in English",
        "Step 2 explanation in English"
      ],
      "steps_ar": [
        "شرح الخطوة الأولى بالعربية",
        "شرح الخطوة الثانية بالعربية"
      ]
    }
  ]
}

Critical Rules:

Ensure Arabic mathematical terms are accurate (e.g., use 'جذر' for root, 'تكامل' for integral).

For the answer field, provide only the value (e.g., "5", "x = 3", "sin(x)").

Do not include markdown formatting (like ```json) in the response, just the raw JSON.

If the input contains multiple problems, create a valid JSON array with multiple objects inside "questions".